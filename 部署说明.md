# 退休者家政联盟 · 网站发布说明

这个网站由两部分组成：
- 前台页面（index.html）——访客浏览、填写预约
- 接收服务（server.js）——保存预约、提供管理后台（admin.html）

## 一、临时公开网址（最快，无需注册）

用 Cloudflare 免费隧道把你的电脑上的网站映射成一个公网网址。
要求：电脑开机、本网站服务在运行、隧道窗口保持开启。

1. 双击「启动网站.bat」启动服务（保持黑色窗口开启）
2. 双击「发布临时网址.bat」——首次运行会自动下载隧道工具（约 55MB），
   之后会得到一个 https://xxxx.trycloudflare.com 的网址
3. 把这个网址发给客户即可；关闭隧道窗口则网址失效

注意：
- 临时网址只在你的电脑开机、两个窗口都开着的时候有效，适合先试运行
- 每次启动会得到一个新的网址；想长期固定网址请看下面「方式 A/B」部署到云平台
- 后台页面：公开网址 + /admin（需要后台密码，密码在 data/admin-password.txt）

## 二、长期公开网址（推荐，部署到免费云平台）

把网站部署到云平台后，网址长期有效，不需要你的电脑开机。
需要你先免费注册一个平台账号（任意一个即可），然后按下面步骤操作。

### 方式 A：Render（免费，最简单）
1. 注册 https://render.com （可用 GitHub 账号登录）
2. 新建 Web Service，选择从 GitHub 仓库部署（先把本文件夹上传到 GitHub）
3. 语言选 Node，启动命令填： node server.js
4. 部署完成后，把服务地址（https://xxx.onrender.com）发给客户
   后台地址：https://xxx.onrender.com/admin

### 方式 B：Vercel（免费，国内访问稍好）
1. 注册 https://vercel.com
2. 把项目上传到 GitHub，在 Vercel 里导入该仓库
3. 框架选 Other，构建命令留空，输出目录留空，启动命令 node server.js
4. 部署完成后得到 https://xxx.vercel.app 地址

### 云平台部署注意事项
- 平台会自动设置 PORT 环境变量，服务会自动监听公网
- 预约数据保存在服务器上的 data/bookings.json；免费平台的磁盘在重启后可能清空，
  重要数据请及时到后台「导出 Excel」备份
- 后台密码在服务器控制台日志里能看到，也可在启动前设置环境变量 ADMIN_PASS 指定

## 三、后台密码

- 本地运行：首次启动自动生成，保存在 data/admin-password.txt，启动窗口也会显示
- 想自己指定：启动前设置环境变量 ADMIN_PASS（例如 set ADMIN_PASS=你的密码）
- 任何人访问 /admin 都需要输入这个密码才能查看预约

## 四、常用操作

| 操作 | 方法 |
| --- | --- |
| 本地启动 | 双击「启动网站.bat」 |
| 查看预约 | 双击「查看预约.bat」（需先启动服务） |
| 导出预约 | 后台页面点「导出 Excel」 |
| 修改后台密码 | 编辑 data/admin-password.txt 后重启服务 |
